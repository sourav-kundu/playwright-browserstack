{
  "devDependencies": {
    "playwright": "^1.10.0"
  },
  "dependencies": {
    "@playwright/test": "0.162.0",
    "chai": "^4.3.4"
  },
  "scripts": {
    "test": "npm run single && npm run local && npm run parallel",
    "single": "node google_search.js",
    "local": "node local_test.js",
    "parallel": "node parallel_test.js"
  }
}
